<button @onclick="Increment" class="btn btn-primary">
    Counter: @_count
</button>

@code {
    private int _count;

    private void Increment()
    {
        _count++;
        Console.WriteLine($"Count: {_count}");
    }
}
